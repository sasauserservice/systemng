import { EntrybyeventPipe } from './entrybyevent.pipe';

describe('EntrybyeventPipe', () => {
  it('create an instance', () => {
    const pipe = new EntrybyeventPipe();
    expect(pipe).toBeTruthy();
  });
});
