import { CalheightDirective } from './calheight.directive';

describe('CalheightDirective', () => {
  it('should create an instance', () => {
    const directive = new CalheightDirective();
    expect(directive).toBeTruthy();
  });
});
