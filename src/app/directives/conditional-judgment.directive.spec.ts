import { ConditionalJudgmentDirective } from './conditional-judgment.directive';

describe('ConditionalJudgmentDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalJudgmentDirective();
    expect(directive).toBeTruthy();
  });
});
