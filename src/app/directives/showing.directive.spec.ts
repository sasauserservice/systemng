import { ShowingDirective } from './showing.directive';

describe('ShowingDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowingDirective();
    expect(directive).toBeTruthy();
  });
});
