import { SasamasonryDirective } from './sasamasonry.directive';

describe('SasamasonryDirective', () => {
  it('should create an instance', () => {
    const directive = new SasamasonryDirective();
    expect(directive).toBeTruthy();
  });
});
