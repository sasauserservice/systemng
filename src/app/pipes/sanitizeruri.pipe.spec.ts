import { SanitizeruriPipe } from './sanitizeruri.pipe';

describe('SanitizeruriPipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizeruriPipe();
    expect(pipe).toBeTruthy();
  });
});
