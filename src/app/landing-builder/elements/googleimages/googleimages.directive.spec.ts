import { GoogleimagesDirective } from './googleimages.directive';

describe('GoogleimagesDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleimagesDirective();
    expect(directive).toBeTruthy();
  });
});
