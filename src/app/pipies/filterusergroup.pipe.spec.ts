import { FilterusergroupPipe } from './filterusergroup.pipe';

describe('FilterusergroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterusergroupPipe();
    expect(pipe).toBeTruthy();
  });
});
